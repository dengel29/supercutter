{# <div class="search-results">
    <template x-if="videoData">
        <template x-if="hasFiltered && !noMatch">

            <p
            class="h-highlight"
            x-text="searchedFilterWord"></p>
            <p>is said</p>
            <p
            class="h-highlight"
            x-text="filteredCaptions.length"></p>
            <p>times in this video.</p><br style="margin-bottom:1em"><p> If you made a supercut from this word, the resulting video would be about</p>
        <p 
        class="h-highlight"
        x-text="Math.round(totalDuration) + ' seconds'"></p>    
        </template>
        <template x-if="noMatch">
            <h4 
            x-text="'⚠️ ' + searchedFilterWord + ' doesn\'t appear in the dialogue of this video, try another word' "></h4>
        </template>
        <div class="captions-wrapper">
        <div class="captions-list">
            <template
                x-for="caption in filteredCaptions"
                :key="caption.start"
            > 
            <a 
                id="caption.start"
                :href="'https://www.youtube.com/watch?v=' + videoData.videoID + '&t=' + caption.start + 's'"
                x-text="caption.start + ' | ' + caption.text"></a>
            </template>
        </div>
        <div class="video-info">
            <h2 x-text="'Subs for ' + videoData.videoTitle"></h2>
            <img :src="videoData.thumbs[0].url" :width="videoData.thumbs[0].width" alt="">
        </div>
        </div>
    </template>
</div> #}